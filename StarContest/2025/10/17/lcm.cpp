#include <bits/stdc++.h>

using namespace std;
constexpr int N = 1e5 + 10;
int n, a[N], b[N];
int main() {
    cin >> n;
    for (int i = 1; i <= n; i++) cin >> a[i] >> b[i];
    for (int i = 1; i <= n; i++) {
        ;
    }
    return 0;
}